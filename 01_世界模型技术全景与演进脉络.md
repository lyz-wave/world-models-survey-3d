# 世界模型（World Models）技术全景与演进脉络

> **文献调研报告 · 第一部分**  
> **编制工具**：`academic-deep-research` 技能套件  
> **当前状态**：完整版交付  
> **覆盖范围**：2018年奠基至今四大技术流派、数学形式化定义与技术路线对比矩阵  

---

## 目录
- [一、 世界模型的形式化定义与数学框架](#一-世界模型的形式化定义与数学框架)
- [二、 技术演进路线图与四大核心流派](#二-技术演进路线图与四大核心流派)
  - [流派 1：基于强化学习的潜空间动力学模型 (Model-Based RL / RSSM)](#流派-1基于强化学习的潜空间动力学模型-model-based-rl--rssm)
  - [流派 2：联合嵌入预测架构 (JEPA: Non-generative Feature Space)](#流派-2联合嵌入预测架构-jepa-non-generative-feature-space)
  - [流派 3：生成式与视频扩散交互世界模拟器 (Generative World Simulators)](#流派-3生成式与视频扩散交互世界模拟器-generative-world-simulators)
  - [流派 4：自动驾驶与具身智能落地架构 (Embodied & Autonomous Driving)](#流派-4自动驾驶与具身智能落地架构-embodied--autonomous-driving)
- [三、 四大流派多维横向技术矩阵](#三-四大流派多维横向技术矩阵)
- [四、 本章学术小结与技术断代分析](#四-本章学术小结与技术断代分析)

---

## 一、 世界模型的形式化定义与数学框架

### 1.1 什么是世界模型？
在认知科学（Kenneth Craik, 1943）中，**心智模型（Mental Model）** 是指生物大脑内部对外部世界运行规律的内部仿真器：生物并不直接对原始感官信号做瞬时反射，而是利用内部模型预测不同行为导致的未来状态与后果，从而在“想象空间”中完成试错、推演与决策。

在人工智能与强化学习语境下，**世界模型（World Model）** 是智能体对其所处环境动力学（Dynamics）的紧凑参数化表征，其本质是学习一个时空条件下的状态转移概率分布。

### 1.2 POMDP 下的形式化描述
真实物理世界通常是**部分可观测的马尔可夫决策过程（POMDP）**，由元组 $(\mathcal{S}, \mathcal{A}, \mathcal{O}, \mathcal{T}, \mathcal{R}, \Omega, \gamma)$ 刻画：
- 真实环境隐状态 $s_t \in \mathcal{S}$（通常不可直接获知）
- 智能体动作 $a_t \in \mathcal{A}$
- 高维高噪声的感官观测 $o_t \in \mathcal{O}$（如相机图像、LiDAR 点云），由观测函数 $\Omega(o_t | s_t)$ 产生
- 真实转移概率 $\mathcal{T}(s_{t+1} | s_t, a_t)$
- 标量奖励信号 $r_t \in \mathcal{R}$

世界模型的核心任务在于，基于智能体历史轨迹 $\tau_t = (o_1, a_1, \dots, a_{t-1}, o_t)$，构建一个计算高效、抗噪且具备因果推断能力的内部紧凑信念状态（Belief State） $\hat{s}_t$，并对未来展开闭环想象：

```text
               +-------------------------------------------+
               |                世界模型内部                 |
               |                                           |
观测 o_t ----> | 表征模型 q(s_t | s_{t-1}, a_{t-1}, o_t)   |
               |                     |                     |
               |                     v                     |
               | 动力学转移 p(s_{t+1} | s_t, a_t) --------> 想象未来 \hat{s}_{t+1}
               |                     |                     |
               |                     +--------+------------+
               |                              |
               |      +-----------------------+-----------------------+
               |      v                                               v
               | 观测重建/解码 p(o_t | s_t)                奖励/价值估计 p(r_t | s_t)
               +--------------------------------------------------------------+
```

### 1.3 核心组件三要素
经典世界模型由三个基础模块耦合而成：
1. **表征模型（Representation / Encoder Model）**：
   $$q_\phi(s_t | s_{t-1}, a_{t-1}, o_t)$$
   将高维高冗余的观测数据（像素、点云）压缩映射到结构化的低维潜空间（Latent Space）。
2. **转移/动力学模型（Transition / Dynamics Model）**：
   $$p_\theta(s_{t+1} | s_t, a_t)$$
   在潜空间内模拟时间流逝与物理演进，无需依赖环境交互即可生成反事实轨迹（Counterfactual Rollouts）。
3. **解码/预测模型（Observation & Reward Model）**：
   $$p_\psi(o_t | s_t), \quad p_\psi(r_t | s_t), \quad p_\psi(\gamma_t | s_t)$$
   负责将隐状态解码为未来感知帧（用于自监督约束）或预测未来奖励与任务终止概率。

---

## 二、 技术演进路线图与四大核心流派

世界模型自 2018 年被重新形式化定义以来，经历了从**玩具环境控制**到**物理世界仿真**，再到**多模态大模型底座**的重大跃迁。

```text
2018                     2020                 2022                 2024                 2026
 |                        |                    |                    |                    |
 +-- World Models (Ha)    |                    |                    |                    |
 |   (VAE + MDN-RNN)      +-- DreamerV1 (RSSM) +-- DreamerV3 (Symlog)+-- Genie (DeepMind)   |
 |                        |   (Latent Actor-   |   (Atari/Minecraft)+-- Sora (OpenAI)    |
 |                        |    Critic)         +-- DayDreamer       +-- V-JEPA 2.0 (Meta)  |
 |                        +-- PlaNet (MPC)     |   (Real Robot RL)  +-- GAIA-2 (Wayve)   |
 |                                             +-- LeCun JEPA Paper +-- OASIS (Decart)   |
 |                                                 (Feature Space)  +-- OccWorld / Drive-WM
```

---

### 流派 1：基于强化学习的潜空间动力学模型 (Model-Based RL / RSSM)
> **核心目标**：最大化采样效率（Sample Efficiency），在潜空间“做梦”以训练智能体策略。

#### 1. 奠基之作：World Models (Ha & Schmidhuber, NeurIPS 2018)
- **架构**：经典的 **V-M-C 架构**。
  - **V 模型 (Vision)**：采用变分自编码器（VAE）将 $64 \times 64 \times 3$ 图像压缩为 32 维高斯潜变量 $z_t$；
  - **M 模型 (Memory)**：混合密度循环神经网络（MDN-RNN），输入 $(z_t, a_t, h_t)$，预测下一时刻隐变量分布 $P(z_{t+1}|z_t, a_t, h_t)$；
  - **C 模型 (Controller)**：极简单层线性控制器，输入拼接向量 $[z_t, h_t]$，输出动作 $a_t$。
- **划时代意义**：首次证明了 Controller 可以**完全在 M 模型生成的“梦境（Dream）”中训练**，并直接迁移回真实环境（CarRacing-v0 与 VizDoom）取得超人类成绩。

#### 2. RSSM 的诞生：PlaNet (Hafner et al., ICML 2019)
- **痛点**：传统纯循环网络（RNN）由于确定性更新导致无法捕获未来随机性；而纯随机状态模型又缺乏维持长程记忆的确定性骨架。
- **创新**：提出**循环状态空间模型（Recurrent State Space Model, RSSM）**。
  - 隐状态解耦为：确定性状态 $h_t = f(h_{t-1}, s_{t-1}, a_{t-1})$（由 GRU/RNN 维持）与随机潜变量 $s_t \sim p(s_t | h_t)$；
  - 采用模型预测控制（CEM / MPC）在线规划，无需训练显式策略即可超越无模型强化学习。

#### 3. 梦境策略梯度的巅峰：Dreamer 进化链
- **DreamerV1 (Hafner et al., ICLR 2020)**：
  - 引入 **Latent Actor-Critic**，利用神经网络的可微性，直接通过长程梦境展开的动力学模型**反向传播策略梯度**（Analytic Policy Gradients），显著超越了无模型 SAC 等算法的样本效率。
- **DreamerV2 (Hafner et al., ICLR 2021)**：
  - **离散分类潜变量（Categorical Latents）**：放弃连续多维高斯分布，改用 32 个 32 类别的离散 One-Hot 向量；
  - **KL 平衡（KL Balancing）**：在先验网络与后验网络更新间引入比例解耦（0.8/0.2），有效防止潜空间信息坍缩；
  - 首次在单个 GPU 上、仅用 2 亿帧在 Atari 55 游戏中超越人类基准和顶级无模型算法（Rainbow）。
- **DreamerV3 (Hafner et al., Nature / arXiv 2023)**：
  - **零调参通用性**：引入 **Symlog 变换**（压缩异常值）、**动态百分位数归一化**（解决跨域奖励尺度相差数个数量级的问题）与自适应正则；
  - 突破了极其稀疏长程的复杂任务：**在 Minecraft 中从零白手起家合成钻石（Diamond Collection）**，无需任何人类专家演示先验数据。
- **DayDreamer (Wu et al., CoRL 2022) & Iris (Micheli et al., ICLR 2023)**：
  - DayDreamer 将 Dreamer 直接部署到真实物理机器狗（Unitree A1）和机械臂，数小时内完成步态自适应；
  - Iris 则首次用 **离散自回归 Transformer** 替代 RSSM 作为世界模型，在 Atari 100k 上刷新 SOTA。

---

### 流派 2：联合嵌入预测架构 (JEPA: Non-generative Feature Space)
> **核心代表**：Yann LeCun, Meta FAIR (I-JEPA, V-JEPA, V-JEPA 2.0)  
> **核心哲学**：**放弃无意义的像素级重建，在抽象特征空间做因果预测。**

#### 2.1 为什么不要像素级生成（Pixel Reconstruction）？
传统生成式世界模型（VAE, Diffusion）通过像素均方误差（MSE）训练解码器，面临不可避免的**维度诅咒与目标不匹配（Objective Mismatch）**：
- 真实世界包含海量不可预测的局部高频熵增细节（如风吹树叶的晃动、水面的波光、地毯上的微小噪点）；
- 像素级模型为了逼真生成每一颗像素，消耗了 90% 以上的模型容量去拟合这些与任务决策完全无关的噪声；
- 一旦面对不确定性，像素模型往往陷入“模糊平均（Blurry Averages）”或“模式崩溃”。

#### 2.2 JEPA 核心设计法则
Yann LeCun 在 2022 年经典论文 *A Path Towards Autonomous Machine Intelligence* 中正式确立了 **Joint-Embedding Predictive Architecture (JEPA)**：
- **Context Encoder $E_x$**：提取已知上下文的抽象高阶语义嵌入 $s_x$；
- **Target Encoder $E_y$**：利用 EMA（指数移动平均）动量更新，提取未来真实观测的抽象特征 $s_y$；
- **Predictor $P_\phi$**：在给定的潜在动作/条件 $z$ 下，直接在**特征空间**预测目标嵌入：$\hat{s}_y = P_\phi(s_x, z)$；
- **损失函数**：仅计算特征空间的距离 $L(\hat{s}_y, s_y)$，彻底剔除像素重建解码器。

```text
               Target Frame y ------------> [ Target Encoder E_y (EMA) ] ----> s_y
                                                                                |
                                                                              Loss (L2/Cosine)
                                                                                |
Context Frame x ----> [ Context Encoder E_x ] ----> s_x                         v
                                                      \--> [ Predictor P ] -> \hat{s}_y
Condition / Action a ---------------------------------/
```

#### 2.3 关键技术迭代：
- **I-JEPA (Assran et al., CVPR 2023)**：在静态图像上验证遮蔽图像特征预测，证明无像素解码即可学到超越 DINO 的语义与结构不变性。
- **V-JEPA (Bardes et al., Meta FAIR, ICLR 2024)**：
  - 扩展至视频域：采用 3D RoPE Transformer 编码时空 Tube，在时空掩码下预测未知时空块的高阶特征；
  - 具备极强的物理规律捕捉能力（如物体遮挡复现、刚体运动轨迹预测），下游冻结特征在细粒度动作分类上达到全新水准。
- **V-JEPA 2.0 (2025/2026 前沿拓展)**：
  - 引入显式动作条件化（Action-conditioning），将 JEPA 结构直接注入机器人操作控制回路，打通“自监督抽象预测 -> 具身强化学习”的闭环。

---

### 流派 3：生成式与视频扩散交互世界模拟器 (Generative World Simulators)
> **核心代表**：Google DeepMind (Genie, UniSim), OpenAI (Sora), Wayve (GAIA), Decart (OASIS)  
> **核心目标**：从海量互联网视频中学习通用物理交互规律，构建逼真可控的虚拟环境与世界引擎。

#### 1. Genie: Generative Interactive Environments (Google DeepMind, 2024)
- **核心定位**：110 亿（11B）参数的基础世界模型，能够将任意文本提示、合成图像或真实照片转化为可无限交互操作的虚拟环境。
- **三大核心架构模块**：
  1. **时空视频分词器 (Spatio-Temporal Video Tokenizer)**：将连续视频压缩为离散的时空视觉 Token；
  2. **潜动作模型 (Latent Action Model, LAM)**：
     - *最大痛点突破*：互联网视频绝大多数没有按键或控制标签（Unlabeled Video）；
     - LAM 通过无监督对比 $P(a_t | x_{1:t}, x_{t+1})$，自行离散聚类出 8 种高解释性的虚拟动作（如向左走、向右跳、跳跃下蹲）；
  3. **自回归动力学预测模型 (Dynamics Model)**：
     - 采用因果 Masked Transformer，在历史帧 Token 与潜在动作序列的驱动下，以自回归方式自发生成具有真实动力学约束的下一帧。

#### 2. GAIA-1 / GAIA-2 (Wayve, 2023-2024)
- **行业定位**：首个专门面向自动驾驶前沿的 90 亿参数多模态生成世界模型。
- **输入多模态**：支持自车方向盘转角、刹车油门控制序列、文本描述（如“雨天路面湿滑、突然冲出一辆自行车”）、历史多视角全景视频。
- **输出能力**：高保真长程推演未来数十秒的多机位 360° 街景变化，能够合成极度危险的边缘极端长尾工况（Corner Cases），为自动驾驶端到端模型提供“在梦境中被撞车”的安全训练场。

#### 3. UniSim: A Universal Interaction Simulator (Google, 2023)
- 结合物理现实感知与通用人机交互，将互联网视频与小规模机器人动作日志对齐；
- 能够准确仿真出刚体碰撞、流体倾倒、织物变形等传统物理仿真引擎（如 Isaac Sim, MuJoCo）极难精准建模的复杂材料形变动力学。

#### 4. OASIS (Decart & Etched, 2024) & Sora (OpenAI, 2024)
- **OASIS**：首个不依赖任何传统游戏引擎代码、**纯粹由神经网络在推理时以 20fps 实时运行**的 Minecraft 交互式世界模型。
- **Sora 启示**：展示了扩散变换器（Diffusion Transformer, DiT）在大规模 Scaling 情况下涌现出的三维几何一致性、持久遮挡保持（Object Permanence）与粗粒度刚体动力学。

---

### 流派 4：自动驾驶与具身智能落地架构 (Embodied & Autonomous Driving)
> **核心代表**：Drive-WM, OccWorld, Copilot4D, 机器人操作策略闭环  
> **核心目标**：从 2D 像素升维到 3D/4D 物理空间，确保空间几何约束与毫米级动作响应。

#### 1. 自动驾驶 4D 占用空间世界模型 (4D Occupancy World Models)
- **代表工作**：**OccWorld (Zheng et al., CVPR 2024)** / **Drive-WM (Wang et al., CVPR 2024)**
- **核心逻辑**：2D 像素图像容易受光照阴影欺骗，而自动驾驶的生死线在于空间占据。
- **机制**：
  - 将 3D 物理空间体素化（Voxelized 3D Occupancy），结合时间轴构成 4D 时空栅格；
  - 世界模型在 4D 栅格中以自回归或扩散方式预测动态车辆、行人的未来占据概率（Occupancy & Flow），与下游规划控制直接对齐。

#### 2. 具身智能策略规划闭环 (Embodied Manipulation & Policy Rollout)
- 将世界模型嵌入模型预测路径积分（MPPI）或蒙特卡洛树搜索（MCTS）循环；
- 智能体在面对未知物体时，在潜空间内推演抓取、推拉、翻转的动力学演化，实现真实环境下的零样本（Zero-shot）或极少样本任务泛化。

---

## 三、 四大流派多维横向技术矩阵

| 评估维度 | 流派 1：潜空间强化学习 (RSSM / Dreamer) | 流派 2：联合嵌入架构 (JEPA) | 流派 3：视频扩散/交互模拟 (Genie / GAIA) | 流派 4：具身与智驾 4D 模型 (OccWorld) |
| :--- | :--- | :--- | :--- | :--- |
| **代表模型** | **DreamerV3, PlaNet, Iris** | **I-JEPA, V-JEPA, V-JEPA 2.0** | **Genie, GAIA-1, UniSim, OASIS** | **Drive-WM, OccWorld** |
| **核心优化目标** | 最大化采样效率与累积奖励 | 提取鲁棒不变的高阶语义特征 | 逼真物理场景与视听交互生成 | 精准 3D 几何占用与碰撞规避 |
| **输入模态** | 图像状态、标量动作、奖励 | 图像帧、连续时空视频 Tube | 文本 Prompt、图像/视频、按键动作 | 多相机视频、LiDAR、自车轨迹指令 |
| **表征空间** | 离散分类潜变量 (Categorical Latents) | 抽象高阶嵌入空间 (Feature Space) | 离散时空 Token (VQ/FSQ) 或扩散潜变量 | 3D Voxel Token / BEV 特征图 |
| **动力学骨干网络** | RSSM (GRU+MDN) / 因果 Transformer | 3D-RoPE ViT Predictor | Spatio-Temporal DiT / Masked Trans | 3D 因果 Transformer / 扩散模型 |
| **解码器需求** | 训练期重建像素/奖励；推理期可丢弃 | **完全无需像素解码器 (Non-generative)**| 强依赖高保真视频解码器 (VAE / DiT) | 解码为 3D 占用栅格与语义类别 |
| **动作条件化** | 显式注入连续或离散控制向量 | 无动作或通过潜空间调制注入 | 显式按键或无监督潜动作模型 (LAM) | 轨迹坐标点、偏航角、线速度输入 |
| **计算与存储开销** | 极轻量（单 GPU 可跑完训练） | 适中（自监督预训练快，无解码负担） | 极重（千卡级 DiT/LLM 规模预训练） | 重（多机位高分辨率时空体素运算） |
| **最大优势** | 策略优化可微、样本效率高、数学严密 | 彻底摆脱像素噪声、不虚构无关细节 | 视觉真实度极高、自然交互性、常识丰富 | 具备真 3D 空间感、无透视形变、贴合控制 |
| **最大瓶颈** | 重建小目标易丢失、环境复杂度受限 | 难以直观还原像素用于人类监看 | 推理延迟高（秒级/帧）、动作存在因果延迟 | 稀疏体素计算昂贵、远距小障碍物感知难 |

---

## 四、 本章学术小结与技术断代分析

回顾世界模型过去八年的演进历程，可以清晰地识别出三次范式转移（Paradigm Shifts）：

1. **第一次范式转移（2018-2021，从外置模拟到隐式做梦）**：
   - 从依赖物理引擎（MuJoCo/Bullet）转变为智能体自主从数据中学习环境规律；
   - 确立了确定性记忆与随机潜变量并存的 **RSSM 范式**，Dreamer 体系将“潜空间可微策略优化”推向极致。
2. **第二次范式转移（2022-2023，像素生成与特征抽象的分流）**：
   - 随着深度强化学习向真实复杂世界进军，“像素级重建是否必要”引发重大哲学分歧；
   - LeCun JEPA 体系提出了“放弃像素、专攻特征因果”的非生成式路线，为自监督世界认知奠定了全新基石。
3. **第三次范式转移（2024 至今，多模态生成大模型与具身世界的合流）**：
   - 生成式大模型（Diffusion / Transformer）展示了海量互联网视频作为常识知识库的巨大潜力；
   - Genie 与 GAIA 证明了“无需动作标注即可逆向工程动作空间”；世界模型正从狭义的“RL 辅助工具”蜕变为通向 AGI 和具身智能物理底座的**通用物理世界模拟器**。

下一章我们将通过严格的 **3-Way Scan (WHY/HOW/WHAT)** 方法，对这四大流派中最为关键的 14 篇里程碑论文展开逐一解剖。
